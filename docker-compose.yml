services:
  app:
    build: .
    container_name: canli-yayin
    restart: unless-stopped
    environment:
      PORT: 3000
      TURN_URLS: ${TURN_URLS:-}
      TURN_USERNAME: ${TURN_USERNAME:-}
      TURN_CREDENTIAL: ${TURN_CREDENTIAL:-}
    expose:
      - "3000"
    ports:
      - "3000:3000"
